# Entire kea_training directory is local-only (training scripts,
# datasets, run results, model weights). Re-add specific files
# with `git add -f <path>` if you want to publish them.
kea_training/

# Cache files
__pycache__/
*.pyc
